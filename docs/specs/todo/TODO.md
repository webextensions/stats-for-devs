# TODO

> TODOs for the stats-for-devs project (forked from the `template-widget` template branch).

* Done (2026-08-04): renamed the project identity from the template
  (`@webextensions/template-javascript-project` -> `stats-for-devs`); widget global is now
  `StatsForDevs`
* Define and implement the actual stats-for-devs functionality, replacing the template's stub API
  (`Greeting` / `useCounter`) - see
  [docs/init/CUSTOMIZE/CUSTOMIZE-source-code-and-tests.md](../../init/CUSTOMIZE/CUSTOMIZE-source-code-and-tests.md)
* Refine the placeholder `description` / `keywords` in `package.json.ts` once the real
  functionality lands
* Before the first `npm publish`: confirm the unscoped npm name `stats-for-devs` is available on
  the registry
